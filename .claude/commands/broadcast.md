Broadcast a signed transaction after simulation and guardrail checks.

Prerequisites: `/simulate` passed, guardrails applied, human confirmation if required.

Sign via KMS/HSM only. Log audit entry with tx hash.
