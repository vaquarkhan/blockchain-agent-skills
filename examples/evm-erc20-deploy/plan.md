# Plan — EVM ERC-20 Deploy

1. `/plan` — select chain (Sepolia/Base Sepolia), MCP evm-rpc-server
2. Compile `contracts/Token.sol`
3. `/simulate` deploy transaction
4. `/broadcast` via KMS signing
5. Verify on Etherscan/Blockscout
