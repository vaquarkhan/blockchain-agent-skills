# Plan — Chain Provider Validation

1. Run existing test suite in `tests/test_chain_providers.py`
2. Extend tests if new chains added to `registry/chains.json`
3. Run `validate-skills.py` for SKILL.md compliance
