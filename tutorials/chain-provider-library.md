# Chain Provider Library Tutorial

The `lib/chain_providers/` package normalizes chain metadata and address validation.

```bash
python tests/test_chain_providers.py
```

Use `resolve_chain("ethereum")` and `validate_address(...)` from consumer code or examples/chain-provider-validation/.
