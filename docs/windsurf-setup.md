# Windsurf Setup

```bash
cp .windsurfrules.example .windsurfrules
scripts/install.sh --tool windsurf --target .
```

Or use VS Code extension in Windsurf: `Blockchain Skills: Install Full Toolkit`.

Read `AGENTS.md` and apply simulate-first policy from `guardrails/transaction-safety.yaml`.
