{
  "openapi": "3.0.0",
  "info": {
    "title": "network-monitoring",
    "version": "0.4.0"
  },
  "paths": {
    "/monitoringPlan": {
      "post": {
        "operationId": "monitoringPlan",
        "summary": "Return network monitoring checklist",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "chain_name": {
                    "type": "string"
                  }
                },
                "required": [
                  "chain_name"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Success"
          }
        }
      }
    }
  }
}
